<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Fundo -->
    <stroke
        android:width="1dp"
        android:color="@color/gray_2" /> <!-- Borda -->
    <corners android:radius="12dp" />
    <corners android:radius="12dp" />

</shape>